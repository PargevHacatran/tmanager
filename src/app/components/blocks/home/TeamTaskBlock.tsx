import { FilterBlock, TopBar, Tasks } from "../home";

export const TeamTaskBlock = () => {
    return (
        <div>
            <TopBar />
            <FilterBlock />
            <Tasks />
        </div>
    );
}